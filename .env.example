# Azure OpenAI reasoning backend
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT=

# Canonical AIDA ElevenLabs voice
# These credentials stay on a trusted AIDA host / Services Gateway and must
# never be embedded in Android or iOS application bundles.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# AIDA Services Gateway — standalone provider boundary
# Normal mobile frontend development should use scripts/start-mobile-dev.ps1,
# which generates AIDA_SERVICES_GATEWAY_TOKEN ephemerally for that session.
# Set an explicit token only when intentionally running the gateway yourself.
AIDA_SERVICES_GATEWAY_TOKEN=
AIDA_SERVICES_GATEWAY_HOST=127.0.0.1
AIDA_SERVICES_GATEWAY_PORT=8787

# Transitional AIDA Mobile Bridge — legacy Desktop AIDA pairing path
# This is not the primary standalone mobile architecture.
AIDA_MOBILE_TOKEN=
AIDA_MOBILE_ALLOW_INSECURE_DEV=false
AIDA_MOBILE_HOST=0.0.0.0
AIDA_MOBILE_PORT=8765
AIDA_MOBILE_ALLOWED_ORIGINS=

# AIDA bug reporting
# Optional override for the registered developer mailbox.
# No sender account, password, API key, subscription, or cloud registration is required.
AIDA_BUG_REPORT_RECIPIENT=AIDAdeveloper@outlook.com

# Optional Phase 1 voice transcription.
# Audio is captured to a temporary local WAV file and is not stored in AIDA memory.
OPENAI_API_KEY=
AIDA_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe

# Artificer Engine — Early Alpha safe defaults.
# Operational telemetry remains on the local device unless the owner explicitly
# changes consent and configures an authorized encrypted receiver.
AIDA_ARTIFICER_ENABLED=true
AIDA_ARTIFICER_MODE=early_alpha
AIDA_ARTIFICER_TELEMETRY_LEVEL=local_only
AIDA_ARTIFICER_AUTO_MAINTENANCE_ENABLED=false
AIDA_ARTIFICER_LOCAL_EXPORT_ENABLED=true
AIDA_ARTIFICER_REVIEW_INTERVAL_SECONDS=21600
AIDA_ARTIFICER_DISPATCH_ENDPOINT=
